{{- if .Values.infrastructure.certManager.deploy -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
  namespace: argocd
spec:
  source:
    chart: cert-manager
    repoURL: {{ .Values.infrastructure.repository }}
    targetRevision: {{ .Values.infrastructure.certManager.version }}
    helm:
      values: |
        environment: {{ toYaml .Values.environment | nindent 8 }}
  destination:
    namespace: gateway
    server: {{ .Values.infrastructure.server }}
  {{- toYaml .Values.infrastructure.defaults | nindent 2 }}
{{- end }}