apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cnpg-backup-s3-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: cnpg-backup-s3-credentials-store
  target:
    name: cnpg-backup-s3-credentials
  data:
  - secretKey: accessKey
    remoteRef:
      key: cnpg-backup-s3-credentials
      property: accessKey

  - secretKey: secretKey
    remoteRef:
      key: cnpg-backup-s3-credentials
      property: secretKey
