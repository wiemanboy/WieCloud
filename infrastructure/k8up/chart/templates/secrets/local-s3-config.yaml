apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: local-s3-config
  namespace: default
spec:
  secretStoreRef:
    kind: SecretStore
    name: {{ .Values.externalsecrets.k8sSecretStore.name }}
  target:
    name: local-s3-config
    template:
      engineVersion: v2
      data:
        seaweedfs_s3_config.json: |
          {
            "identities": [
              {
                "name": "admin",
                "actions": ["Admin"],
                "credentials": [
                  {
                    "accessKey": "{{ `{{ .accessKey }}` }}",
                    "secretKey": "{{ `{{ .secretKey }}` }}"
                  }
                ]
              }
            ]
          }
  data:
  - secretKey: accessKey
    remoteRef:
      key: local-s3-credentials
      property: accessKey

  - secretKey: secretKey
    remoteRef:
      key: local-s3-credentials
      property: secretKey
