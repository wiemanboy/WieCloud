apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: local-backup-s3-config
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: local-backup-s3-secret-store
  target:
    name: local-s3-config
    template:
      engineVersion: v2
      data:
        seaweedfs_s3_config.json: |
          {
            "identities": [
              {
                "name": "admin",
                "actions": ["Admin"],
                "credentials": [
                  {
                    "accessKey": "{{ `{{ .accessKey }}` }}",
                    "secretKey": "{{ `{{ .secretKey }}` }}"
                  }
                ]
              }
            ]
          }
  data:
  - secretKey: accessKey
    remoteRef:
      key: local-backup-s3-credentials
      property: accessKey

  - secretKey: secretKey
    remoteRef:
      key: local-backup-s3-credentials
      property: secretKey
