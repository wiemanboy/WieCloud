apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn-frontend
spec:
  parentRefs:
    - name: {{ .Values.environment.gateway.name }}
      sectionName: {{ .Values.environment.gateway.listener }}
      namespace: {{ .Values.environment.gateway.namespace }}

  hostnames:
    - storage.{{ .Values.environment.hostname }}
    
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /

      backendRefs:
        - name: longhorn-frontend
          namespace: {{ .Release.Namespace }}
          port: 80

      filters:
        {{- if .Values.environment.gateway.authentication.enabled }}
        {{- if .Values.environment.gateway.authentication.basicAuth }}
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: {{ .Values.environment.gateway.authentication.basicAuth.name }}
        {{- end }}
        {{- end }}
