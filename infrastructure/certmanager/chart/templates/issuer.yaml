apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.certificate.issuer }}
spec:
  acme:
    email: {{ .Values.certificate.email }}
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: {{ .Values.certificate.secret }}
    solvers:
    - dns01:
        cloudflare:
          email: {{ .Values.certificate.email }}
          apiTokenSecretRef:
            name: {{ .Values.certificate.cloudflare.token.secret.name }}
            key: {{ .Values.certificate.cloudflare.token.secret.key }}
